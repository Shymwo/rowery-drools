package com.rowery

import com.rowery.Fact;
import com.rowery.Engine;


rule "test-start"
	when
	then
		
		Engine.Question("who");
		
end

rule "fake"
	when 
		Fact(N("who"), V(1));
	then

		Engine.Question("sidewheels");
end
