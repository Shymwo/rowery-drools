package com.rowery

import com.rowery.Fact;
import com.rowery.Engine;


rule "test-start"
	when
	then
		
		Engine.Question("who");
		
end
rule "fake1"
	when 
		Fact(name("who"), val==1);
	then

		Engine.Answer("rower1");
end
rule "fake"
	when 
		Fact(name("who"), val==1);
	then

		Engine.Answer("rower1");
end
